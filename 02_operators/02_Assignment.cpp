#include <iostream>
using namespace std;

int main (){
    int a = 10;
    int b = 5;
    a += 5; // This is equivalent to a = a + 5;
    cout << "a = " << a << endl;
    /*
    -=  // This is equivalent to a = a - b; 
    +=  // This is equivalent to a = a + b;
    *=  // This is equivalent to a = a * b; 
    /=  // This is equivalent to a = a / b; 
    */  
    
}